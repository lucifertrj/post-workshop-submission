## STRATEGIES & INSIGHTS

[str-00052] helpful=1 harmful=0 :: [str-99998] helpful=1 harmful=0 :: Ensure that all column names containing spaces in the database schema are enclosed in backticks (`` ` ``) or have spaces replaced with underscores to avoid syntax errors.

[str-00051] helpful=1 harmful=0 :: Ensure that all values used in WHERE clause conditions match exactly with those stored in the database schema to avoid no records being returned.

[str-00050] helpful=1 harmful=0 :: Ensure that all column names for free meal count across different age groups are correctly referenced in the database schema. Verify that column names like 'FreeMealCountAges5_17' exist and match exactly with the schema.

[str-00049] helpful=1 harmful=0 :: [str-00049] helpful=1 harmful=0 :: Ensure that all table names match exactly as they are defined in the database schema; there should be no case differences or typos when referencing tables in SQL queries.

[str-00048] helpful=1 harmful=0 :: `Free_Meal_Count_K_12` does not exist in the `frpm` table. The correct column name might be something like `FreeMealCountK12` or a similar variation.

[str-00047] helpful=1 harmful=0 :: Ensure that all grouping columns are correctly specified in the GROUP BY clause to avoid inaccurate aggregation results.

[str-00046] helpful=1 harmful=0 :: Ensure that all column names match exactly as they are defined in the database schema; there should be no case differences or typos when referencing columns in SQL queries.

[str-00045] helpful=1 harmful=0 :: Ensure that subquery column aliases are correctly referenced in the outer query to avoid 'column not found' errors.

[str-00044] helpful=1 harmful=0 :: Ensure that the correct column names for school type in the 'schools' table are used instead of assuming generic names.

[str-00043] helpful=1 harmful=0 :: Ensure that queries returning single rows are formatted correctly into lists to match the expected output structure, especially when using aggregate functions like COUNT(). This can be done by wrapping the result in a list using SQL's array functions or ensuring that Python code handling the results explicitly formats them as expected.

[str-00042] helpful=1 harmful=0 :: Ensure that all column names match exactly as they are defined in the database schema; there should be no case differences or typos when referencing columns in SQL queries.

[str-00041] helpful=1 harmful=0 :: Ensure that all school name columns are correctly referenced from the appropriate tables. If the `School` column is not present, use the `School Name` column instead.

[str-00039] helpful=1 harmful=0 :: [str-00040] helpful=1 harmful=0 :: Ensure that the SQL query checks for the existence of a column in the schema before attempting to access it. If the column does not exist, use an alternative or handle the absence gracefully.

[str-00038] helpful=1 harmful=0 :: Ensure that the 'County Code' column in the `frpm` table is correctly referenced as `CDSCode` by verifying the schema details.

[str-00037] helpful=1 harmful=0 :: Ensure that the SQL query references the correct column names by verifying the schema of all relevant tables before executing queries.

[str-00036] helpful=1 harmful=0 :: Ensure that the `frpm` table contains the expected columns before executing queries and adjust the SQL query accordingly.

[str-00035] helpful=1 harmful=0 :: [str-00035] helpful=1 harmful=0 :: Always verify which column represents the desired information before selecting it in the SQL query. For example, if you need district codes instead of names, ensure 'DistrictCode' is selected.

[str-00034] helpful=1 harmful=0 :: Ensure that all column names are enclosed in backticks or double quotes if they contain spaces or are reserved keywords to avoid syntax errors.

[str-00033] helpful=1 harmful=0 :: `District Type` should be enclosed in backticks or double quotes to correctly reference the column name.

[str-00031] helpful=1 harmful=0 :: [str-00032] helpful=1 harmful=0 :: Ensure that all SQL queries include conditions relevant to the question being asked, especially when dealing with specific time periods and events like school openings.

[str-00030] helpful=1 harmful=0 :: Ensure that all types of charter school funding are included in the calculation when determining the percentage of 'Locally funded' schools in Santa Clara County.

[str-00029] helpful=1 harmful=0 :: Ensure that the california_schools county column is correctly referenced as 'County Name' with spaces to avoid syntax errors.

[str-00028] helpful=1 harmful=0 :: Ensure that the SQL query generated by the agent is not an empty list before passing it to the execute() function.

[str-00027] helpful=1 harmful=0 :: Ensure that the GROUP BY and ORDER BY clauses in SQL queries use direct column references instead of table aliases to avoid syntax errors.

[str-00026] helpful=1 harmful=0 :: Ensure that the postal street address and mailing street columns are correctly handled in queries to avoid unexpected results when joining or filtering these fields.

[str-00025] helpful=1 harmful=0 :: Ensure that all grouping and ordering clauses in SQL queries are correctly applied to avoid returning incorrect results.

[str-00024] helpful=1 harmful=0 :: Ensure that all columns referenced in the SQL query are correctly spelled and formatted according to the database schema.

[str-00023] helpful=1 harmful=0 :: Ensure that all school name columns are correctly referenced from the appropriate tables. If the `School` column is not present, use the `School Name` column instead.

[str-00022] helpful=1 harmful=0 :: Ensure that the data type for columns storing integer values is defined as an integer type to avoid unnecessary decimal precision in results.

[str-00021] helpful=1 harmful=0 :: Ensure that the grouping in SQL queries aligns with the specific metric being measured to avoid incorrect aggregation results.

[str-00020] helpful=1 harmful=0 :: Ensure that all columns referenced in the SQL query exist within the specified tables. If necessary, join additional tables or modify the query to include valid columns.

[str-00019] helpful=1 harmful=0 :: Ensure that column names containing spaces or reserved keywords are properly quoted to avoid syntax errors. For example: `SELECT "T1.Educational Option Type" FROM frpm AS T1 INNER JOIN satscores AS T2 ON T1.CDSCode = T2.cds WHERE T2.rtype = 'S' AND T2.AvgScrMath IS NOT NULL ORDER BY T2.AvgScrMath DESC LIMIT 1`

[str-00018] helpful=1 harmful=0 :: Ensure that the `satscores` table is not empty and contains entries for 'Reading' before running SQL queries. Also, ensure that there are active districts in the `schools` table with scores available.

[str-00017] helpful=1 harmful=0 :: Verify that the CDSCode column is used consistently across tables to ensure accurate joins and relationships between tables. Additionally, when aggregating data and grouping by a specific column, it is crucial to verify that the grouping logic is correct to avoid returning incorrect results.

[str-00016] helpful=1 harmful=0 :: Verify that all columns referenced in the SQL query exist within the specified tables. If necessary, join additional tables or modify the query to include valid columns.

[str-00015] helpful=1 harmful=0 :: Ensure that all column names referenced in the SQL query are correctly spelled and formatted according to the database schema.

[str-00014] helpful=1 harmful=0 :: Ensure that all columns referenced in the SQL query exist within the specified tables. If necessary, join additional tables or modify the query to include valid columns.

[str-00013] helpful=1 harmful=0 :: Ensure that null or undefined values in funding type columns are handled appropriately in SQL queries by using COALESCE or a similar function to provide default values.

[str-00012] helpful=1 harmful=0 :: Ensure that column names with spaces are properly escaped using backticks (`) to avoid syntax errors. For example: `Free Meal Count(K-12)` and `Enrollment(K-12)`.

[str-00011] helpful=1 harmful=0 :: Ensure that parentheses are not used around conditions in the WHERE clause. For example, use `WHERE Free Meal Count (K-12) > 500 AND Free Meal Count (K-12) < 700` instead of `(Free Meal Count (K-12) > 500 AND Free Meal Count (K-12) < 700)`.

[str-00010] helpful=1 harmful=0 :: Ensure that numeric values in SQL queries are compared as integers or strings without quotes, depending on the data type stored in the database schema.

[str-00009] helpful=1 harmful=0 :: Ensure that the correct classification and interpretation of data in tables like `schools` are verified before running SQL queries to avoid discrepancies.

[str-00008] helpful=1 harmful=0 :: Ensure that the expected columns are correctly selected from the appropriate tables in the SQL query.

[str-00007] helpful=1 harmful=0 :: Verify that the expected data exists in the database before running a query, especially when dealing with specific conditions like counting records below a certain threshold.

[str-00006] helpful=1 harmful=0 :: Ensure that the `satscores` table is not empty and contains entries for 'Reading'. Also, ensure that there are active districts in the `schools` table with scores available.

[str-00005] helpful=1 harmful=0 :: Ensure that arithmetic operations in SQL queries include spaces around operators like '+'. For example, use `Enrollment (K-12) + Enrollment (Ages 5-17)` instead of `Enrollment (K-12) + Enrollment (Ages 5-17)`. Additionally, ensure that all numbers in the query are either prefixed with a space or enclosed in parentheses if they are part of an expression.

[str-00004] helpful=1 harmful=0 :: Use backticks (\") around column names with spaces to avoid parsing errors in SQL queries.

[str-00003] helpful=1 harmful=0 :: Ensure that column names are correctly referenced without quotes unless they contain special characters or spaces, and verify their existence in the database schema.

[str-00002] helpful=1 harmful=0 :: Ensure that the column names match the actual schema in the database. Use the DESCRIBE or SHOW COLUMNS command to list all columns and their data types.

[str-00001] helpful=0 harmful=0 :: Interpret "Exclusively virtual" as records where Virtual = 'F', ensuring only entries explicitly marked with 'F' are included.


(no entries yet)

## FORMULAS AND CALCULATIONS
[calc-00001] helpful=1 harmful=0 :: Verify that the aggregation method and filtering criteria are correctly applied when calculating averages in SQL queries. Cross-check data for anomalies or missing values.
