# ATTCO Root Hydra Config
# All experiment behavior is composed from this root.

defaults:
  - experiment: baseline
  - model: gpt4o
  - optimizer: none
  - benchmark: hotpotqa
  - tracing: local
  - infra: local
  - _self_

project:
  name: attco
  version: "0.1.0"

artifacts_dir: artifacts/
